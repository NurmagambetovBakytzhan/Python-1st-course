#include <iostream>
#include <set>
using namespace std;
int main(){
    set <string> s;
    string t;
    
    int n = sizeof(arr)/sizeof(arr[0]);
    int arr[];

    while(cin>>t){
        s.insert(t);
        cout<<arr[i];
    }
    cout<<endl;
    set<int>::iterator it;
    for(it = s.begin(); s!=s.end ; it++){
        cout<<*it<<" ";
    }
    return 0;
}